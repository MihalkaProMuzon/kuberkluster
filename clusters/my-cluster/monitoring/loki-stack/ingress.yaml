apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - komplikter-sub3.servehttp.com
      secretName: tlss-sub3-prod-secret
  rules:
    - host: komplikter-sub3.servehttp.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loki-stack-grafana-tls-svc
                port:
                  number: 443